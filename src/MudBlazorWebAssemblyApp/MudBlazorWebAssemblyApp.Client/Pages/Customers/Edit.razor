@page "/customers/{id:int}/edit"
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Components.Authorization
@inject IAsyncCustomerService Api
@inject IAsyncRegionService RegionApi
@inject ISnackbar Snackbar
@inject NavigationManager Nav

@* @inject IAuthorizationService AuthorizationService
@inject AuthenticationStateProvider AuthStateProvider
@attribute [Authorize(Policy = "VIP")]
 *@

@* <AuthorizeView>
   <Authorized>
       Witaj @context.User.Identity.Name !
   </Authorized>

   <NotAuthorized>
       Nie masz dostepu
   </NotAuthorized>

</AuthorizeView> *@


<MudText Typo="Typo.h3">Edit Customer</MudText>

@if (customer is null)
{
    <MudProgressCircular Color="Color.Primary" Indeterminate="true" />
}
else
{
    <CustomerEditForm customer="@customer" regions="@regions" OnValidSubmit="@OnValidSubmit" />
    
    <MudButton ButtonType="ButtonType.Button" Color="Color.Success" Variant="Variant.Filled" @onclick=@GenerateDocument
    >Wygeneruj dokument</MudButton>

}

@code {

    [Parameter]
    public int Id { get; set; }

    private Customer? customer;

    private List<Region>? regions;


    protected override async Task OnInitializedAsync()
    {
        regions = await RegionApi.GetAll();      
    }

    protected override async Task OnParametersSetAsync()
    {
        customer = await Api.GetById(Id);
    }

    private async Task OnValidSubmit(EditContext context)
    {
        try
        {
            await Api.Update(customer!);

            Snackbar.Add($"Zmodyfikowana klienta {customer!.Name}", Severity.Success);

            /*
            var authState = await AuthStateProvider.GetAuthenticationStateAsync();            

            var email = authState.User.FindFirst(c => c.Type == "email")?.Value;

            // TODO: send email to

            */

                        Nav.NavigateTo("/customers");
        }
        catch(Exception e)
        {
            Snackbar.Add(e.Message, Severity.Error);
        }




    }

    private async Task GenerateDocument()
    {
        
    }

}
